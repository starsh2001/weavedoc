# WeaveDoc — Go 재작성 계획

> 결정: 2026-08-04 (사용자). §11 결정표에 같은 날 행으로 기록.
> 이 문서는 **무엇을·어떤 순서로·무엇으로 증명하며** 옮기는지를 정한다. 코드는 아직 없다.

## 0. 왜 (실측)

이 결정을 낳은 숫자는 전부 2026-08-04에 이 머신에서 측정한 것이다.

| 프로세스 1개 생성 | 실측 |
|---|---|
| MSYS `/usr/bin/true` | 319ms |
| MSYS `$( )` 서브셸 | 380ms |
| 네이티브 `hostname.exe` | 51ms |
| 네이티브 `cmd.exe /c exit` | 49ms |

weavedoc 호출 1회의 고정 floor는 MSYS에서 **2,980ms**(3회: 2984·2977·3002)다. 분해하면 `bash -n` 파싱 ~640ms + `SCRIPT_DIR`/`ROOT` ~170ms + config 로드 ~1,900ms. 마지막 항은 `cfg()`가 호출당 `$(grep|head|sed)` = 4 fork를 내고 최상위에서 3번 불리기 때문이다. **13개 프로세스 × ~230ms ≈ 2,980ms**로 측정치와 맞는다.

| 342케이스 전체 스윕 | 실측 |
|---|---|
| MSYS (이 머신) | **36분 47초** |
| Linux 컨테이너 (같은 머신, WSL2) | **9초** |
| Ubuntu CI `regression-linux` | 40초 |

**하드웨어가 아니라 POSIX 에뮬레이션이 비용이다.** 같은 코드가 같은 머신에서 245배 차이 난다.

### 속도보다 큰 이유 — 결함 클래스

2026-08-04 하루에 수리한 결함이 전부 한 클래스였다: awk가 로케일 따라 다르게 읽음(v0.3.5) · scope의 grep이 ko 로케일에서 `Binary file matches`를 돌려줌(v0.3.6) · **bash `read`가 멀티바이트 로케일에서 유효하지 않은 바이트를 담은 줄을 통째로 버림**(v0.3.7, 3-OS 중 Linux만 실패). 전부 **텍스트 처리를 외부 도구에 위임했는데 그 도구가 플랫폼·로케일마다 다르게 행동**해서 생긴다. 단일 바이너리에서는 이 클래스가 존재하지 않는다.

부수 효과로 배포도 단순해진다. 지금 macOS CI는 `brew install bash gawk gnu-sed coreutils`가 선행되어야 하고, `preflight_gnu`는 그 상황을 런타임에 방어하는 코드다. 정적 바이너리에는 둘 다 필요 없다.

## 1. 무엇을 바꾸지 않는가 (불변 계약)

재작성은 **동작을 옮기는 일이지 다시 설계하는 일이 아니다.** 다음은 그대로 간다.

- **CLI 표면** — 명령 이름·인자·플래그·`--json` 스키마
- **종료 코드** — 0/1/2의 의미
- **출력 바이트** — 사람이 읽는 줄, `examined:` 회계선, 진단 코드(`[FM-MISSING]` 등 86종), 경고 문구
- **온디스크 형식** — `.weavedoc/schema`가 SoT. frontmatter·ledger 6열·seal·gaps 문법·라벨 꼬리 전부 불변
- **원칙** — fail-closed, 격리 = 증거 없음, 규칙 하나에 철자 하나, 사람이 판정하는 자리는 기계가 넘보지 않음

바꾸는 것은 **구현 언어와 프로세스 모델뿐이다.**

## 2. 증명 수단 — 342개 케이스가 곧 명세

이 프로젝트가 운이 좋은 지점: `tests/regress.sh`의 342 케이스는 전부 **CLI 블랙박스**다. 광산을 만들고, 명령을 실행하고, stdout과 종료 코드를 본다. 내부를 모른다.

그래서 **케이스를 그대로 두고 실행 대상만 바꿔** 재작성을 검증할 수 있다. 파리티 계약:

> 같은 광산 · 같은 명령 → **stdout 바이트 동일 · 종료 코드 동일**

stderr는 대조하지 않는다(구현마다 경고 문구가 다를 수밖에 없고, v0.3.7에서 이미 stdout만 보도록 정리했다).

**단, 세 부류는 옮길 수 없다** — 지금 확인된 것:
- `bash -n` 파싱 검사, `preflight_gnu`(GNU 도구 존재 확인) — 대상이 사라짐. Go에서는 `go vet`/빌드가 그 자리를 대신한다.
- `rm` 셰임 고장 주입(`block_consecrate_validate_fail_final_unremovable`) — PATH 셰임이 안 통한다. Go에서는 주입 지점을 인터페이스로 열어 테스트한다.
- `meta_doc_sync`·`tests/doccheck.sh` — bash 소스에서 dispatch 표를 파싱해 문서와 대조한다. 파싱 대상을 Go 소스(또는 생성된 표)로 바꾼다.

이 세 부류는 **소리 없이 빠지면 안 된다.** 각각 대체 케이스를 만들고, 대체하지 못하면 계획서에 남긴다.

## 2.1 배포 경계 — 테스트는 배포물이 아니다

`tests/make-manifest.sh`가 정의하는 배포 번들은 정확히 이것뿐이다:

```
.weavedoc/{bin/weavedoc, schema, READ.md, FORMATS.md, templates/*}
.claude/skills/weavedoc-*
```

`tests/`도, 이 계획서도, `IMPROVEMENT_PLAN.md`도, `CHANGELOG.md`도 들어가지 않는다.

**따라서 회귀 하네스는 bash로 남긴다.** 재작성 대상은 **배포되는 바이너리 하나**뿐이고, `regress.sh`의 3,000줄은 옮기지 않는다. 이것이 이 재작성을 감당 가능하게 만드는 두 번째 이유다(첫째는 §2의 블랙박스 케이스).

**그 대가로 스윕은 초 단위가 되지 않는다.** 하네스가 bash로 남으면 케이스마다 광산을 복사하는 비용이 그대로 남고, weavedoc이 네이티브가 되는 순간 **그 복사가 지배항이 된다**:

| MSYS 스윕 342케이스 | |
|---|---|
| 지금 (bash weavedoc) | **36분 47초** |
| Go 바이너리 + bash 하네스 | 광산 복사 682ms×342 = 233s · 호출 ~500×50ms = 25s · 하네스 오버헤드 → **약 5분** |

즉 **스윕은 ~7배, 개별 명령은 ~60배**다. 명령 쪽이 실사용이 체감하는 숫자이고(eclypse `validate` 19초 → 1초 미만 기대), 스윕 쪽은 그 다음 병목이 광산 복사로 옮겨간다는 뜻이다. 복사까지 없애려면 하네스도 손대야 하는데 — 하네스를 제품 바이너리 안에 넣으면(`weavedoc selftest`) 픽스처를 만드는 코드와 검증받는 코드가 같아져 시험의 독립성을 잃는다. **지금은 하지 않는다**; 5분이 문제가 되면 하네스만 별도 프로그램으로 옮기는 것을 §11에 재상정한다.

## 3. 배포 모델 (결정)

**선택**: 릴리스는 플랫폼별 바이너리를 발행하고, 사용자 프로젝트의 `.weavedoc/bin/`에는 **그 호스트용 한 개만** 놓는다. 이름은 `weavedoc`(Windows는 `weavedoc.exe`).

- 릴리스 자산: `weavedoc-{linux,darwin,windows}-{amd64,arm64}`
- `init`/업그레이드가 호스트에 맞는 것을 골라 `.weavedoc/bin/weavedoc`로 놓는다
- 호출 형태: `bash .weavedoc/bin/weavedoc <cmd>` → **`.weavedoc/bin/weavedoc <cmd>`**. MSYS는 확장자 없는 경로를 `.exe`로 해석하므로 **세 OS에서 철자가 하나**로 유지된다

**기각한 대안**:
- **bash 런처 셰임**(`.weavedoc/bin/weavedoc`을 스크립트로 두고 플랫폼 바이너리를 exec) — 호출 형태를 한 글자도 안 바꿔도 되지만, **MSYS bash 기동만 384ms 실측**이다. 호출 1회가 ~50ms가 아니라 ~430ms가 되어(9배) 전환 이유의 대부분을 잃는다.
- **모든 플랫폼 바이너리를 `.weavedoc/`에 동봉** — 복사 한 번으로 끝나는 배포 모델에는 맞지만 프로젝트마다 15~25MB가 들어간다. 대부분의 사용자는 한 플랫폼만 쓴다.

**되돌림 조건**: `init`이 호스트를 잘못 판별하는 사례가 반복되거나, 한 프로젝트를 여러 OS에서 공유하는 실사용이 나오면 전 플랫폼 동봉을 재상정한다.

**영향 범위(실측)**: 호출 형태가 박힌 곳 — `tests/regress.sh` 46곳(`vrun` 1곳 + 직접 45곳), 스킬 8파일 11곳, 문서 36파일. 스킬과 문서는 사용자가 읽는 면이라 같은 커밋에서 함께 바꾼다.

## 4. 순서

각 단계는 **끝날 때 스위트가 green**이어야 한다. 기존 규율(red-first · 불변 계약 · 유닛마다 커밋 · 침습 유닛은 콜드 diff 리뷰)은 그대로 적용한다.

**0단계 — 하네스 이중화 (Go 코드 0줄)**
`regress.sh`가 실행 대상을 환경변수로 받게 한다(`WD_BIN`, 기본값은 현행 bash). 이 단계가 끝나도 342/342는 bash로 green이어야 한다. 여기서 아무것도 빨라지지 않는다 — **다음 단계 전부를 채점할 저울을 먼저 만드는 것**이다.

**1단계 — 뼈대 + 가장 싼 명령**
Go 모듈, dispatch 표, `version`·`lang`·`locale`. 이 셋만 Go로 돌리고 해당 케이스를 파리티로 통과시킨다.
→ **여기서 첫 실측**: Go 바이너리의 실제 기동 시간. 추정 ~50ms가 맞는지 이 시점에 확인한다.

**2단계 — 바닥 (읽기 기반)**
config·schema·frontmatter 값 규칙·섹션/주석 리더·ledger 행 필터·digest. 명령이 아니라 **모든 명령이 딛는 바닥**이고, bash 판에서 드리프트가 반복해 터진 자리다. Go에서는 규칙마다 함수 하나 + 표 기반 테스트로 못 박는다.

**3단계 — 읽기 명령**
`status` · `scope` · `census` · `gaps` · `pull` · `impact`. 부작용이 없어 파리티 실패가 광산을 다치게 하지 않는다.

**4단계 — 쓰기 명령**
`attest` · `reindex` · `retag` · `seal-review`. 원자적 쓰기·롤백은 bash 판이 이미 어렵게 배운 것이라 그 규칙을 그대로 옮긴다.

**5단계 — 어려운 둘**
`validate`(진단 86종) · `consecrate`(상태 기계·in-flight marker·postcondition). 마지막에 두는 이유는 바닥과 읽기 명령이 다 검증된 뒤라야 실패 원인이 좁아지기 때문이다.

**6단계 — 전환**
기본 대상을 Go로 바꾸고, 스킬·문서·CI를 같은 커밋에서 넘긴다. bash 판은 한 릴리스 동안 `legacy/`에 남겨 두고 파리티 검증용으로만 쓴다.

## 5. 지금 막혀 있는 것

- **Go 툴체인이 이 머신에 없다**(`go: not installed`). Docker는 살아 있고 이미 빌드·테스트에 쓸 수 있음이 증명됐다(스윕 9초). 로컬 설치 없이 `golang` 이미지로 빌드·테스트할 수 있으니 **차단 요소는 아니지만**, 개발 편의를 위해 로컬 설치를 권한다.
- **Go 버전·모듈 경로·최소 지원 OS/아키텍처** 미정. 1단계 착수 전에 정한다.

## 6. 그동안의 bash 판

전환은 며칠 단위가 아니다. 그 사이에도 실사용은 계속되므로:

- `cfg()`의 fork 수리(스윕 −43%, 실사용 명령 −1.9초)는 **전환과 무관하게 값을 한다.** 30분 작업이고 매일 회수된다.
- 그 외 bash 판 신규 기능은 멈춘다. 결함 수리만 한다 — 두 판을 동시에 키우면 파리티 계약이 움직이는 과녁이 된다.
