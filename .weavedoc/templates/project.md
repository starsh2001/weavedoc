---
version: 3
language: {ko|en}
roles: []          # material roles — gather assigns silently as materials arrive; you can edit anytime
tone:              # optional; each document's plan sets its own tone — fill only for a standing project tone
authority: []      # optional, legacy-inert: no conflict is resolved by role rank — every ruling is the user's, per entry. Kept legal so migrated mines stay valid; validate checks only that entries are declared roles.
required_tags: []  # optional: tags that must have at least one truth (e.g. [범위, 일정, 대금, 품질])
# gap_density: minimal          # minimal | dense — the 설정 밀도 policy gaps asks ONCE on its first
#   run and persists here; later gaps runs read it back instead of re-guessing per gap.
---
<!--
  The mine's character reveals itself as materials are gathered — this body starts
  as a one-line placeholder and grows. AI drafts; you edit.
  See .weavedoc/FORMATS.md.
-->

{one-line placeholder — filled in as the mine grows}
