# UPGRADING — v2 광산을 schema 3으로

이 런타임은 **v3 전용**입니다. `version: 2`인 광산은 모든 일반 명령이 한 가지 답 — "먼저 이주하세요" — 만 주고 아무것도 판정하지 않습니다(v2 카드는 v3 필수 키를 전부 만족해서, 게이트가 없으면 v2 광산이 v3 규칙 아래 깨끗하게 **오통과**합니다).

## 백업 → 변환 → 검증

백업은 **깨끗한 git 워크트리**입니다. 이주기는 그것을 확인만 하고(read-only `git status`), 복원을 대신 실행하지 않습니다 — 되돌리는 말은 언제나 같습니다:

```bash
git restore . && rm -rf .weavedoc-state
```

```bash
node .weavedoc/bin/weavedoc.mjs upgrade --check     # 분류·차단 항목·high-water 보고 (read-only; --dry-run 동일)
node .weavedoc/bin/weavedoc.mjs upgrade --apply     # 변환 + 보존식·exact-validate 검증 (clean git 필수)
```

## 무엇이 일어나나

v2 status 축은 전부 소진됩니다 — 카드마다 처분이 정확히 하나씩:

| v2 상태 | 처분 |
|---|---|
| `ok` (남는 전부) | 카드 유지, `status`·`conflict_with`·`resolution`·`superseded` **줄만** 제거(그 외 바이트 불변) |
| `discarded` / `retracted` | 카드 삭제(과거는 git), 그 카드의 verify 장부 행도 함께 제거 |
| `conflict` | `.weavedoc-state/conflicts.json`의 **무손실 후보**로 이동 — 살아남는 `ok` 짝이 있으면 그 카드가 entry의 target, 전원 conflict면 `targets: []`(아직 아무도 안 정한 상태). **이주는 충돌을 해결하지 않습니다** — 보관 장소만 옮기므로, 이주 직후 validate가 그 entry로 빨간 것이 정상이고, 사람이 판정해 entry를 삭제할 때까지 유지됩니다 |

`superseded`는 카드 종류가 아니라 **승자 카드의 필드**입니다 — 지우면 현재 사실이 사라지므로 절대 삭제 축이 아닙니다. `decided_by: machine`인 v2 resolution은 **보고만** 됩니다: 기계가 골랐던 값이 현행 카드로 남는데, 재론할지는 이주 후 사용자의 몫입니다.

새로 생기는 것: `.weavedoc-state/{conflicts.json, id-sequences.json}` (allocator의 next는 삭제 **전** 전수 스캔의 high-water 위 — 문서 인용·장부·로그의 id 토큰까지 셉니다), 그리고 `project.md`·`config.yaml` 모두 `version: 3`.

## 첫 쓰기 전에 멈추는 것들 (v2에서 정리하고 재실행 — 결정 저장 장치는 없습니다)

- **`status: unsupported` 카드** — v3에서는 "존재=정본"이라 근거 깨진 카드가 조용히 승격됩니다. 재-grounding하거나 삭제하세요.
- **`resolution.type: attribute` 쌍(승인된 병기)** — 기록만 벗기면 "놓친 모순"과 구별 불가한 카드 두 장이 남습니다. "둘 다 맞다"는 언제나 숨은 축(시간·관점·정의·범위, 최후는 출처 귀속)이 있습니다 — 그 축을 claim에 새겨 분리한 뒤 재실행.
- **삭제/이동될 카드를 인용하는 문서** — 인용을 먼저 고치세요. 매달린 인용은 id 규율이 막으려는 바로 그 부패입니다.
- (경고) **마지막 담지 카드가 떠나는 required_tag** — 이주 후 REQTAG-EMPTY가 예고되므로 apply가 거부합니다.

## 이주가 채우는 것 — 자료 유입 장부 (intake ledger)

번들 `2026-08-08.28`부터 자료에도 검증 축과 같은 장치가 붙습니다: `materials/intake-ledger.tsv` — **자료가 어떻게 들어왔는지**를 기록하는 기계 소유 장부. 지금까지 이 축에는 기계적 관문이 하나도 없었고, 그래서 **사용자가 건넨 자료와 에이전트가 만들어낸 자료가 구별되지 않았습니다.** 카드를 쓰는 주체가 자료도 쓰기 때문에 날조된 자료는 구조적으로 완벽합니다 — 인용도 맞고, coverage도 닫히고, 봉인도 통과합니다.

기존 광산은 전부 이 장부보다 앞서므로, **이주가 자료마다 `legacy-unbound` 행 하나를 채웁니다.** 실제 역사이되 **어떤 바이트도 묶지 않는** 행이고, 선언(`declared`)과는 어디서나 따로 세어집니다.

- **v2 → v3 이주**: 스키마 이주와 같은 `--apply` 안에서 함께 채워집니다.
- **이미 v3인 광산**: `weavedoc upgrade`가 더 이상 "이미 v3"로 즉시 끝나지 않고, 채울 행이 남았는지 **확인한 뒤** 알려 줍니다. `--apply`로 채웁니다. 이쪽은 **깨끗한 git 워크트리를 요구하지 않습니다** — v2→v3는 카드를 지우고 frontmatter를 고쳐 쓰는 *변형*이라 git이 유일한 되돌리기지만, 이 경로는 기계 소유 장부에 행을 **덧붙일 뿐** 다른 것은 건드리지 않습니다(역은 그 행들을 지우는 것). 이 런타임의 다른 덧붙이기 쓰기(`attest`, `conflict add`, `seal-review`)도 같은 이유로 백업을 요구하지 않으며, 요구했다면 **모든 기존 광산이 반드시 한 번 실행해야 하는 명령**에 — 작업 중이라 워크트리가 더러운 바로 그때 — 마찰을 놓는 셈이 됩니다.

채우고 나면 `validate`의 `MAT-UNDECLARED`는 제 뜻을 갖습니다 — *장부가 생긴 뒤에 나타났는데 선언되지 않은 자료*. 그 전까지는 전 자료가 걸리므로, **이 검사는 경고이며 절대 차단이 아닙니다**(모든 현장 광산을 한꺼번에 빨갛게 만드는 검사는 하루 만에 아무도 읽지 않게 됩니다 — `CLAUDE-BLOCK-STALE`에 적용한 판단 그대로).

채운 뒤에는 두 갈래가 있고, 서로 다른 것을 줍니다:

- **`weavedoc intake --anchor-existing "<무엇을 보증하는지>"`** — 남은 backlog 전체를 **지금 바이트로** 묶습니다. 검증이 아닙니다("오늘 바이트는 이것이었다"만 기록하고, 판정어도 `declared`가 아니라 `anchored`입니다). 대신 이 순간부터 원본이든 광산의 사본(`converted.md`)이든 바뀌면 `scope`가 `stale`로 부릅니다 — 묶기 전에는 그 편집이 **아무 흔적도 남기지 않습니다.** 실행 전에 트리가 보증할 상태인지 먼저 확인하세요(더러운 편집이 있으면 그게 기준선으로 채택됩니다). 이미 행이 있는 자료는 — stale이어도 — 건드리지 않습니다.
- **`weavedoc intake <id> "<어떻게 들어왔는지>"`** — 유래까지 기록하는 실제 선언. **위험 순으로 하나씩** 하세요. 원본이 실제로 있는지 확인하지 않은 선언은 장부에 거짓을 한 줄 더 쓰는 일입니다.

원본이 애초에 없는 자료(대조할 대상이 없다고 사용자가 재정한 건)는 `intake --no-source <id> "<사용자의 재정>"`으로 남깁니다 — 원본은 안 묶지만 사본은 묶습니다.

## 이주가 손대지 않는 것 — 프로젝트 CLAUDE.md의 포인터 블록

이주기는 **광산 안**만 씁니다. `init`이 프로젝트 `CLAUDE.md`에 심어 둔 `<!-- weavedoc:begin -->` 블록은 광산 밖이라 이주가 건드리지 않으므로, **이주 후 그 블록은 여전히 이전 번들의 것**입니다.

그냥 두면 안 되는 이유: CLAUDE.md는 세션마다 **어떤 파일보다 먼저** 주입됩니다. 낡은 요약이 거기 있으면 조용히 낡는 데서 끝나지 않고 **읽는 사람을 프라이밍**합니다 — 실제로(2026-08-13) 블록에 v2 문구가 남아 있던 광산에서, 세션이 지시대로 `READ.md`를 먼저 읽고도 *v2* 규약을 파일 내용인 양 보고했고, 없는 필드를 인용했으며, 그 모델 위에 사용자 선택지를 만들었습니다.

이주 직후 `validate`가 이것을 이름으로 알려 줍니다:

```
warning: [CLAUDE-BLOCK-STALE] CLAUDE.md's weavedoc block differs from .weavedoc/templates/claude-block.md …
```

수리는 **`weavedoc-init` 재실행**(reconfigure)입니다 — 인터뷰를 다시 받고 마커 사이를 번들이 싣고 있는 `.weavedoc/templates/claude-block.md`로 다시 씁니다. 마커 **바깥**에 적어 둔 프로젝트 고유 문장은 그대로 남습니다. (경고이지 차단이 아닙니다: 낡은 포인터는 광산 무결성 결함이 아니므로 발행을 막지 않습니다.)

## 검증 — 이주의 보증

- 보존식: v2 카드 수 = 남은 카드 + 삭제 + 이동, 누락·중복 0.
- 이주 직후 validate는 **예측된 CONFLICT-OPEN 한 줄만** 내야 합니다 — 그 외 무엇이든 나오면 이주는 실패로 보고되고 복원 문구를 출력합니다.

## v1 광산은?

이 런타임에는 v1 리더가 없습니다. 고정 브리지 런타임 **v0.5.21 (commit `0257167`)** 의 `weavedoc upgrade`로 v1→v2를 먼저 밟은 뒤, 이 런타임으로 v2→v3를 밟습니다. (v1→v2에서 무엇이 바뀌는지는 그 브리지 체크아웃의 UPGRADING.md가 정본입니다.)
