# 이관 직후 독립 재실행 기록 — 2026-08-02

Phase 0에서 `notes/regress.sh` → `tests/regress.sh` 이관(byte-identical) 후, **이관된 위치에서** 전체 suite를 독립 재실행한 기록. 계획서 §7.3의 계약(실행 환경 명시 · 실행된 케이스 ID 명시 · skipped를 PASS에 포함하지 않음)을 따른다.

## 결과

**passed 182 · failed 0 · not yet run 0** — 전건 통과.

실행된 케이스: [case-manifest.txt](case-manifest.txt)의 182개 전부 (block 101 · pass 54 · acct 26 · meta 1). 이번 세션에서 미실행으로 남긴 케이스는 없다.

## 환경

| 항목 | 값 |
|---|---|
| commit | 0486329 (improve/phase0) |
| bundle | 2026-08-02.2 · manifest digest `795fdda02fb4…` |
| OS/shell | Windows 10 Pro 19045 · Git Bash (MINGW64) · bash 5.2.26 · gawk 5.0.0 · sed 4.9 |
| 병렬도 | -j6 |

## 실행 로그 (청크 분할)

한 호출 시간 제한 때문에 `--resume`으로 나눠 실행했다. 전 구간 같은 commit·bundle이므로 resume cache 재사용은 유효하다 (다른 commit 간 재사용 금지는 WD-QA-002의 Phase 2 작업).

| # | 명령 | 집계 |
|---|---|---|
| 1 | `tests/regress.sh acct` | 26/26 (wall 3m19s) |
| 2 | `--resume pass_` | 54/54 |
| 3 | `--resume -n40 block` | 40 (누적 120) |
| 4 | `--resume -n30 block` | 70/101 (block 기준) |
| 5 | `--resume -n31 block` | 101/101 (block 기준) |
| 6 | `--resume` (무필터) | **182/182 최종 집계** |

## golden 캡처

같은 정의의 fixture(별도 경로에 `mkpristine` 재현)에서 6개 명령의 현재 출력을 [golden/](golden/)에 저장했다: `validate` · `census` · `scope` · `status` · `gaps` · `version` — 전부 exit 0, validate는 `✓ validate: all checks passed`.

## 한계 (정직성 기록)

- 이 기록은 **현재 suite의 통과**를 증명한다. 케이스 assertion이 기대 문자열 1개짜리로 느슨하다는 문제(WD-QA-003)는 그대로이며, exact diagnostic 검증은 Phase 2에서 도입한다.
- 단일 머신·단일 OS 실행이다. 3-OS matrix는 Phase 2 CI에서.
