# Not part of the WeaveDoc distribution — local scaffolding & working notes
.bmad-core/
.claude/commands/
.claude/settings.local.json
# Codex mirrors .claude/commands/ (ignored above) into its own skill root; the bundle
# ships only .agents/skills/weavedoc-*, which is exactly what make-manifest.sh selects.
.agents/skills/source-command-*/
notes/
weavedoc-for-hammoc.md
dashboard-prototype.html

# Local tool state (session/prompt history — never publish)
.hammoc/
.qlaude/

# OS noise
Thumbs.db
desktop.ini
.DS_Store
